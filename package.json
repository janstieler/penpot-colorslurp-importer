{
  "name": "colorslurp-importer",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "preview": "vite-live-preview --port 4400 --host 0.0.0.0",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "@penpot/plugin-styles": "^1.4.2",
    "@penpot/plugin-types": "^1.4.2"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vite-live-preview": "^0.4.0"
  }
}
